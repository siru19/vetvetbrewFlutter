import 'package:flutter/material.dart';

class TableBookingScreen extends StatelessWidget {
  const TableBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("TableBooking"),
      ),
      body: Center(
        child: Text("TableBooking"),
      ),
    );
  }
}
