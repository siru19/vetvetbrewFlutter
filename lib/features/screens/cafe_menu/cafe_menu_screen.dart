import 'package:flutter/material.dart';

class CafeMenuScreen extends StatelessWidget {
  const CafeMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CafeMenuScreen"),
      ),
      body: Center(
        child: Text("CafeMenuScreen"),
      ),
    );
  }
}
